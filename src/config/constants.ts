// Centralized application constants

// Default avatar image used across the app
export const DEFAULT_AVATAR = "/avatars/default-avatar.png";

// Cloudinary upload folder for profile photos (must match src/lib/cloudinary.ts)
export const CLOUDINARY_UPLOAD_FOLDER = "hrms/profiles";
